<Window x:Class="NoteManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Note Manager Pro" Height="500" Width="800" Background="#F0F2F5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="White" BorderBrush="#DDD" BorderThickness="0,0,1,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Пошук нотаток" FontWeight="Bold" Margin="0,0,0,5"/>
                    <DockPanel>
                        <Button Content="Знайти" Click="SearchButton_Click" DockPanel.Dock="Right" Padding="5,0" Background="#007ACC" Foreground="White" BorderThickness="0"/>
                        <TextBox x:Name="SearchInput" Height="25" VerticalContentAlignment="Center"/>
                    </DockPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="A-Z" Click="SortTitleButton_Click" Width="50" Margin="0,0,5,0" Background="#EEE"/>
                    <Button Content="Нові" Click="SortDateButton_Click" Width="50" Background="#EEE"/>
                </StackPanel>

                <ListBox x:Name="NotesList" Grid.Row="2" SelectionChanged="NotesList_SelectionChanged" DisplayMemberPath="Title"/>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Назва" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="TitleInput" FontSize="16" Padding="5"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="Теги (через кому)" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="TagsInput" Padding="5"/>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock Text="Зміст" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="ContentInput" AcceptsReturn="True" TextWrapping="Wrap" Height="200" Padding="10" VerticalScrollBarVisibility="Auto"/>
            </StackPanel>

            <UniformGrid Grid.Row="3" Columns="4" Height="40" Margin="0,20,0,0">
                <Button Content="Нова" Click="NewNoteButton_Click" Background="#28A745" Foreground="White" Margin="0,0,5,0" FontWeight="Bold"/>
                <Button Content="Відміна" Click="UndoButton_Click" Background="#FFC107" Foreground="Black" Margin="0,0,5,0" FontWeight="Bold"/>
                <Button Content="Зберегти" Click="SaveNoteButton_Click" Background="#007ACC" Foreground="White" Margin="0,0,5,0" FontWeight="Bold"/>
                <Button Content="Видалити" Click="DeleteNoteButton_Click" Background="#DC3545" Foreground="White" FontWeight="Bold"/>
            </UniformGrid>
        </Grid>
    </Grid>
</Window>
